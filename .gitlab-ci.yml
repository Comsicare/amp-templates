secret-scan:
  stage: test
  image:
    name: zricethezav/gitleaks:latest
    entrypoint: [""]
  tags: [docker]
  script:
    - gitleaks detect --source . --no-git -v
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push"'
